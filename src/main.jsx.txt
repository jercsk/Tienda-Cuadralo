import React from 'react'
import ReactDOM from 'react-dom/client'
// Importamos el componente App. Asegúrate de que en GitHub el archivo 
// se llame exactamente "App.jsx" (sin .txt)
import App from './App.jsx'

const rootElement = document.getElementById('root');

// Verificamos que el elemento root exista en el index.html antes de renderizar
if (rootElement) {
  ReactDOM.createRoot(rootElement).render(
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
}